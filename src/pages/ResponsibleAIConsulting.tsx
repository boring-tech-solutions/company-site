import ServicePageTemplate from "@/components/services/ServicePageTemplate";
import { responsibleAIConsulting } from "@/data/servicePages";

const ResponsibleAIConsulting = () => {
  return <ServicePageTemplate page={responsibleAIConsulting} />;
};

export default ResponsibleAIConsulting;
